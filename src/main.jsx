import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{ArrowRight,CalendarDays,Check,ChevronDown,Menu,MessageCircle,Phone,X}from'lucide-react';import'./styles.css';
const logo='/logo-ynr-luxury.png';const fallback={settings:{brand:'YNR Luxury Rent',tagline:'Location de voitures sportives et de luxe pour professionnels et particuliers',phone:'06 48 02 37 74 · 07 45 45 41',whatsapp:'33648023774',instagram:'https://instagram.com/ynr_location',snapchat:'https://t.snapchat.com/ECABDZ05',email:'ynr.location@gmail.com'},vehicles:[{id:'v1',name:'BMW M3 Competition',category:'Berline sportive',price:490,description:'Sportive, précise et confortable.',photos:['https://images.unsplash.com/photo-1555215695-3004980ad54e?auto=format&fit=crop&w=1400&q=90']}],blocked:[]};
const api=async(u,o)=>{const r=await fetch('/api'+u,{credentials:'include',headers:{'Content-Type':'application/json'},...o});if(!r.ok){const body=await r.json().catch(()=>({}));throw Error(body.message||`Erreur serveur (${r.status})`)}return r.status===204?null:r.json()};
function Public(){const[d,setD]=useState(fallback),[menu,setMenu]=useState(false),[selected,setSelected]=useState(null),[sent,setSent]=useState(false);useEffect(()=>{api('/public').then(setD).catch(()=>{})},[]);const s=d.settings,wa=`https://wa.me/${s.whatsapp}`;return <div className="site"><header><a className="wordmark" href="#top">YNR <b>LUXURY RENT</b></a><nav className={menu?'open':''}><a href="#why" onClick={()=>setMenu(false)}>Pourquoi nous</a><a href="#fleet" onClick={()=>setMenu(false)}>Véhicules</a><a href="#contact" onClick={()=>setMenu(false)}>Réserver</a></nav><button className="menu" aria-label={menu?'Fermer le menu':'Ouvrir le menu'} aria-expanded={menu} onClick={()=>setMenu(!menu)}>{menu?<X size={24}/>:<Menu size={24}/>}</button><a className="header-call" href="tel:+33648023774"><Phone size={15}/> Appeler</a></header><div className="ticker" aria-label="Nos services"><div className="ticker-track"><span>LOCATION DE VOITURES</span><span>MARBELLA · PARIS</span><span>SPORT & LUXURY</span><span>RÉSERVATION RAPIDE</span><span>LOCATION DE VOITURES</span><span>MARBELLA · PARIS</span><span>SPORT & LUXURY</span><span>RÉSERVATION RAPIDE</span></div></div><main id="top"><section className="hero"><div className="hero-logo"><img src={logo} alt="YNR Luxury Rent"/></div><div className="hero-copy"><p className="eyebrow">LOCATION DE VOITURES SPORTIVES & DE LUXE</p><h1>Louez une voiture<br/><em>qui marque.</em></h1><p className="lead">Pour un rendez-vous professionnel, un événement ou simplement le plaisir de conduire. Choisissez votre véhicule, envoyez vos dates : nous vous répondons rapidement.</p><div className="phone-strip"><a href="tel:+33648023774"><Phone size={16}/>06 48 02 37 74</a><a href="tel:+337454541"><Phone size={16}/>07 45 45 41</a></div><div className="hero-actions"><a className="button primary" href="#fleet">Voir les véhicules <ArrowRight size={17}/></a><a className="whatsapp" href={wa} target="_blank" rel="noreferrer"><MessageCircle size={17}/> WhatsApp</a></div></div></section><section className="proof-band"><span><Check/> Réponse rapide</span><span><Check/> Véhicules contrôlés</span><span><Check/> Service professionnel</span><span><Check/> Marbella · Paris</span></section><section className="statement" id="why"><p className="eyebrow">POURQUOI YNR LUXURY RENT</p><h2>Pas de perte de temps.<br/><em>Pas de mauvaise surprise.</em></h2><p>Nous faisons mieux que les plateformes impersonnelles : un interlocuteur réel, des conditions expliquées clairement et une voiture préparée avant votre arrivée. Professionnels comme particuliers bénéficient du même niveau d’exigence.</p><div className="reason-grid"><article><b>01</b><h3>Réponse humaine</h3><p>Votre demande est traitée directement par notre équipe, pas par un formulaire oublié.</p></article><article><b>02</b><h3>Flotte exigeante</h3><p>Chaque véhicule est sélectionné, nettoyé et contrôlé avant chaque départ.</p></article><article><b>03</b><h3>Simple et transparent</h3><p>Prix, dépôt et disponibilité sont annoncés avant de confirmer.</p></article></div></section><section className="fleet-section" id="fleet"><div className="section-head"><div><p className="eyebrow">LA COLLECTION</p><h2>Votre prochaine <em>expérience.</em></h2></div><a href="#contact">Réserver <ArrowRight size={15}/></a></div><div className="fleet-grid">{d.vehicles.map(v=><article className="car-card" key={v.id}><img src={v.photos?.[0]} alt={v.name}/><div className="car-info"><p>{v.category}</p><h3>{v.name}</h3><strong>À partir de {v.price}€ <small>/ jour</small></strong><button className="button primary" onClick={()=>{setSelected(v);document.querySelector('#contact').scrollIntoView({behavior:'smooth'})}}>Demander ce véhicule <ArrowRight size={15}/></button></div></article>)}</div></section><section className="contact-section" id="contact"><div><p className="eyebrow">RÉSERVER EN 2 MINUTES</p><h2>Vos dates.<br/><em>Votre voiture.</em></h2><p>Choisissez vos dates et laissez vos coordonnées. Nous confirmons la disponibilité et vous rappelons pour finaliser.</p><div className="direct"><a href={wa} target="_blank" rel="noreferrer"><MessageCircle/> WhatsApp immédiat</a><a href="tel:+33648023774"><Phone/> 06 48 02 37 74</a><a href="tel:+337454541"><Phone/> 07 45 45 41</a></div></div>{sent?<div className="success"><Check size={30}/><h3>Demande envoyée.</h3><p>Nous revenons vers vous rapidement.</p></div>:<Request vehicles={d.vehicles} selected={selected} onSent={()=>setSent(true)}/>}</section><section className="faq"><p className="eyebrow">QUESTIONS FRÉQUENTES</p><h2>Tout est clair<br/><em>avant de partir.</em></h2>{['Comment réserver un véhicule ?','Quels documents sont nécessaires ?','Le dépôt de garantie est-il remboursable ?'].map((q,i)=><details key={q}><summary>{q}<ChevronDown size={16}/></summary><p>{['Envoyez vos dates via le formulaire, WhatsApp ou téléphone. Nous confirmons ensuite la disponibilité.','Une pièce d’identité et un permis de conduire valide sont demandés. Les conditions exactes sont précisées avant confirmation.','Oui, selon les conditions convenues avec le véhicule choisi.'][i]}</p></details>)}</section></main><footer><div><div className="footer-name">YNR LUXURY RENT</div><p>Location de voitures sportives et de luxe.</p></div><div className="footer-links"><a href={s.instagram} target="_blank" rel="noreferrer">Instagram</a><a href={s.snapchat} target="_blank" rel="noreferrer">Snapchat</a><a href={`mailto:${s.email}`}>{s.email}</a></div></footer></div>}
function Request({vehicles,selected,onSent}){const[start,setStart]=useState(''),[end,setEnd]=useState('');return <form className="request" onSubmit={async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{await api('/requests',{method:'POST',body:JSON.stringify({...f,start,end,vehicle:selected?.name||f.vehicle})});e.currentTarget.reset();onSent()}catch(x){alert(x.message)}}}><label>Véhicule<select name="vehicle" autoComplete="off" defaultValue={selected?.name}>{vehicles.map(v=><option key={v.id}>{v.name}</option>)}</select></label><div className="form-grid"><label>Début<input name="start" autoComplete="off" required type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Fin<input name="end" autoComplete="off" required type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label></div><label>Nom complet<input name="name" autoComplete="name" required placeholder="Votre nom"/></label><label>Téléphone<input name="phone" autoComplete="tel" required placeholder="06 00 00 00 00"/></label><label>E-mail<input name="email" autoComplete="email" required type="email" placeholder="vous@exemple.com"/></label><button className="button primary">Vérifier la disponibilité <ArrowRight size={17}/></button></form>}
function FleetManager({data,setData}){const[tab,setTab]=useState('vehicles'),[editing,setEditing]=useState(null),[notice,setNotice]=useState(''),[preview,setPreview]=useState([]);const save=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const files=[...e.currentTarget.querySelector('input[type=file]').files];const uploaded=await Promise.all(files.map(file=>new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=async()=>{try{const r=await api('/uploads',{method:'POST',body:JSON.stringify({data:reader.result})});resolve(r.url)}catch(err){reject(err)}};reader.onerror=reject;reader.readAsDataURL(file)})));const payload={...f,price:Number(f.price),deposit:Number(f.deposit),photos:uploaded.length?uploaded:String(f.photos||'').split('\\n').map(x=>x.trim()).filter(Boolean)};const r=editing?await api('/vehicles/'+editing.id,{method:'PATCH',body:JSON.stringify(payload)}):await api('/vehicles',{method:'POST',body:JSON.stringify(payload)});setData(d=>({...d,vehicles:editing?d.vehicles.map(v=>v.id===r.id?r:v):[r,...d.vehicles]}));setEditing(null);setNotice('Véhicule enregistré');e.currentTarget.reset()};const remove=async id=>{if(!confirm('Supprimer ce véhicule ?'))return;await api('/vehicles/'+id,{method:'DELETE'});setData(d=>({...d,vehicles:d.vehicles.filter(v=>v.id!==id)}))};return <div className="fleet-manager"><div className="admin-tabs"><button className={tab==='vehicles'?'active':''} onClick={()=>setTab('vehicles')}>Flotte</button><button className={tab==='requests'?'active':''} onClick={()=>setTab('requests')}>Demandes ({data.requests?.length||0})</button><button className={tab==='calendar'?'active':''} onClick={()=>setTab('calendar')}>Calendrier</button></div>{tab==='vehicles'&&<><form className="vehicle-editor" onSubmit={save}><h2>{editing?'Modifier le véhicule':'Ajouter un véhicule'}</h2><div className="editor-grid"><input name="name" defaultValue={editing?.name||''} placeholder="Nom du véhicule" required/><input name="category" defaultValue={editing?.category||''} placeholder="Catégorie"/><input name="price" type="number" min="0" defaultValue={editing?.price||0} placeholder="Prix / jour (€)"/><input name="deposit" type="number" min="0" defaultValue={editing?.deposit||0} placeholder="Caution (€)"/><textarea name="description" defaultValue={editing?.description||''} placeholder="Description"/><label className="photo-upload">Photos locales<input name="photoFiles" type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>setPreview([...e.target.files].map(file=>URL.createObjectURL(file)))}/><small>JPG, PNG ou WEBP · 8 Mo maximum par image · cadrage automatique 16:10</small>{preview.length>0&&<div className="photo-preview-grid">{preview.map((src,i)=><img key={src} src={src} alt={`Aperçu ${i+1}`}/>)}</div>}</label><textarea name="photos" defaultValue={editing?.photos?.join('\\n')||''} placeholder="Ou URLs existantes, une par ligne"/></div><div className="editor-actions"><button className="button primary">{editing?'Enregistrer':'Ajouter le véhicule'}</button>{editing&&<button type="button" className="button" onClick={()=>setEditing(null)}>Annuler</button>}</div>{notice&&<p className="success-note">{notice}</p>}</form><div className="vehicle-admin-list">{data.vehicles.map(v=><article className="vehicle-admin-card" key={v.id}>{v.photos?.[0]&&<img src={v.photos[0]} alt=""/>}<div><h3>{v.name}</h3><p>{v.category} · {v.price} € / jour · caution {v.deposit} €</p><button className="text-button" onClick={()=>setEditing(v)}>Modifier</button><button className="danger-button" onClick={()=>remove(v.id)}>Supprimer</button></div></article>)}</div></>}{tab==='requests'&&<div className="admin-panel"><h2>Demandes clients</h2>{data.requests?.map(r=><div className="admin-row" key={r.id}><b>{r.vehicle}</b><span>{r.name} · {r.phone} · {r.start} → {r.end}</span><small>{r.status}</small></div>)}</div>}{tab==='calendar'&&<div className="admin-panel"><h2>Calendrier de disponibilité</h2><p>Les dates réservées ou bloquées apparaissent ici.</p>{data.blocked?.length?data.blocked.map((b,i)=><div className="admin-row" key={i}><b>{b.vehicleId}</b><span>{b.start} → {b.end}</span></div>):<p>Aucun créneau bloqué.</p>}</div>}</div>};function Admin(){const[logged,setLogged]=useState(false),[data,setData]=useState(null),[error,setError]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState('');const load=()=>api('/admin').then(d=>{setData(d);setLogged(true)}).catch(()=>{});useEffect(()=>{load()},[]);const login=async e=>{e.preventDefault();setError('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});setLogged(true);load()}catch(e){setError('Identifiants invalides ou serveur indisponible.')}};if(!logged)return <main className="admin-login"><form onSubmit={login}><p className="eyebrow">YNR LUXURY RENT · ADMIN</p><h1>Accès propriétaire</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label>Mot de passe<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<p className="form-error">{error}</p>}<button className="button primary">Ouvrir le tableau de bord <ArrowRight size={16}/></button></form></main>;return <main className="admin-page"><header><a className="wordmark" href="/">YNR <b>LUXURY RENT</b></a><button className="button" onClick={async()=>{await api('/auth/logout',{method:'POST'});setLogged(false)}}>Se déconnecter</button></header><section className="admin-content"><p className="eyebrow">ESPACE PROPRIÉTAIRE</p><h1>Pilotage de votre flotte.</h1>{data?<><FleetManager data={data} setData={setData}/><div className="admin-grid"><article><strong>{data.requests?.length||0}</strong><span>Demandes reçues</span></article><article><strong>{data.vehicles?.length||0}</strong><span>Véhicules actifs</span></article><article><strong>{data.blocked?.length||0}</strong><span>Créneaux bloqués</span></article><div className="admin-panel"><h2>Demandes récentes</h2>{data.requests?.length?data.requests.slice(0,8).map(r=><div className="admin-row" key={r.id}><b>{r.vehicle}</b><span>{r.name} · {r.start} → {r.end}</span><small>{r.status}</small></div>):<p>Aucune demande pour le moment.</p>}</div><div className="admin-panel"><h2>Flotte</h2>{data.vehicles?.map(v=><div className="admin-row" key={v.id}><b>{v.name}</b><span>{v.category}</span><strong>{v.price} € / jour</strong></div>)}</div></div></>:<p>Chargement des données…</p>}</section></main>};function App(){return location.pathname==='/admin'?<Admin/>:<Public/>}createRoot(document.getElementById('root')).render(<App/>);
